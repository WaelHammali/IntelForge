[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "intelforge"
version = "3.0.0"
description = "Autonomous penetration-testing & AI reconnaissance framework orchestrated with LangGraph"
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Wael Hammali" }]
keywords = ["pentest", "osint", "reconnaissance", "langgraph", "security", "nmap"]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: Information Technology",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
]
dependencies = [
    "langgraph>=1.2,<2",
    "langchain>=1,<2",
    "langchain-groq>=1,<2",
    "langchain-openai>=1,<2",
    "pydantic>=2.7,<3",
    "pydantic-settings>=2.3,<3",
    "click>=8.1",
    "requests>=2.31",
    "rich>=13.7",
    "pyyaml>=6.0",
    "defusedxml>=0.7",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
    "pre-commit>=3.7",
    "types-requests",
    "types-PyYAML",
    "types-defusedxml",
]

[project.scripts]
intelforge = "intelforge.cli:main"

[project.urls]
Homepage = "https://github.com/WaelHammali/IntelForge"
Repository = "https://github.com/WaelHammali/IntelForge"
Issues = "https://github.com/WaelHammali/IntelForge/issues"
Changelog = "https://github.com/WaelHammali/IntelForge/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/intelforge"]
artifacts = ["src/intelforge/py.typed"]

[tool.hatch.build.targets.wheel.force-include]
"src/intelforge/prompts" = "intelforge/prompts"

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C4", "PTH", "RUF"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]

[tool.ruff.lint.isort]
known-first-party = ["intelforge"]

[tool.mypy]
python_version = "3.11"
packages = ["intelforge"]
mypy_path = "src"
strict = false
warn_unused_ignores = true
warn_redundant_casts = true
disallow_untyped_defs = false
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["langgraph.*", "langchain.*", "langchain_core.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
# LangGraph's StateGraph.add_node overloads do not accept a plain
# Callable[[State], dict]; the graph is exercised end-to-end in tests instead.
module = ["intelforge.graph.pipeline"]
disable_error_code = ["call-overload"]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-q --strict-markers"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.coverage.run]
source = ["src/intelforge"]
branch = true
